
const u = require("wlj-utilities");

const lineIsProofStep = require("../../library/lineIsProofStep.js");
const index = require("../../index.js");

u.scope(__filename, x => {
    // TODO: Fix broken test
    //u.assert(false);
});
